# 🎭 ระบบผังที่นั่งโรงละคร ลิเกไลฟ์อินเธียเตอร์ (Likay Live in the Theater Seating Chart System)

ระบบผังที่นั่งแบบโต้ตอบ (Interactive Seating Chart System) สำหรับการแสดงลิเกไลฟ์อินเธียเตอร์ (Likay Live In Theater) พัฒนาด้วย **Laravel Framework (Blade/Eloquent)** ร่วมกับ **Vanilla JavaScript** และ **CSS Grid/Flexbox** พร้อมเชื่อมต่อระบบเรียลไทม์ผ่าน **Pusher/Laravel Echo** ช่วยให้สามารถเลือกจองที่นั่ง อัปโหลดหลักฐานการโอนเงิน (Slip) แสดงสถานะการเลือกที่นั่งแบบเรียลไทม์ และจัดการข้อมูลการจองได้อย่างปลอดภัยด้วยระบบธุรกรรม (Transaction)

---

## 🏗️ โครงสร้างไฟล์ในระบบ (System Architecture)

ระบบประกอบด้วยไฟล์หลักๆ ดังต่อไปนี้:

1. **เส้นทาง (Routes):** `routes/web.php` (บรรทัดที่ 59-72) และลงทะเบียนสิทธิ์ช่องสัญญาณ real-time ใน `routes/channels.php`
2. **คอนโทรลเลอร์ (Controller):** `app/Http/Controllers/LikayLiveInTheaterController.php`
3. **โมเดล (Models):**
   - `app/Models/LikaySeat.php` (ข้อมูลเก้าอี้และสถานะการจอง)
   - `app/Models/LikayBooking.php` (ข้อมูลประวัติการจองและหลักฐานโอนเงิน)
   - `app/Models/User.php` (กำหนดบทบาทสิทธิ์ `ROLE_LIKAY` และ `ROLE_MANAGER`)
4. **แผนผังที่นั่งบริการ (Service):** `app/Services/LikaySeatMap.php` (ระบบจัดการแมปปิ้งเก้าอี้และโซนสีแบบ Dynamic)
5. **เหตุการณ์อัปเดตเรียลไทม์ (Broadcast Event):** `app/Events/LikaySeatStatusUpdated.php` (เหตุการณ์แจ้งเตือนเมื่อเบราว์เซอร์อื่นจอง, ยกเลิก หรือกำลังคลิกที่นั่ง)
6. **ไฟล์ไมเกรชัน (Database Migrations):** `database/migrations/2026_06_01_000002_create_likay_concert_tables.php`
7. **หน้าแสดงผล (Blade Views):**
   - หน้าหลักแอดมิน: `resources/views/likay-band.blade.php` (สำหรับแอดมิน/เมเนเจอร์จองที่นั่ง ปลดบล็อก และจัดการราคา)
   - หน้าแสดงผลสาธารณะ: `resources/views/likay-public.blade.php` (หน้าจอ Read-only สำหรับแสดงคิวงานหน้าโรงละคร/ผู้ชมทั่วไป อัปเดตสดแบบเรียลไทม์)
   - หน้าล็อกอิน: `resources/views/likay-login.blade.php`
   - หน้าแสดงรายการประวัติการจอง: `resources/views/likay-bookings.blade.php` (ตารางค้นหา ประวัติ และดูรูปสลิป)

---

## 🗄️ โครงสร้างฐานข้อมูล (Database Schema)

ระบบใช้ตารางในฐานข้อมูล 3 ตารางเพื่อแบ่งหน้าที่ในการจองที่นั่ง ประวัติลูกค้า และการกำหนดราคาแยกตามโซน:

### 1. ตาราง `likay_seats` (จัดเก็บสถานะเก้าอี้ที่นั่ง)
ทำหน้าที่จัดเก็บว่าที่นั่งใดถูกจองแล้วและเชื่อมกับประวัติการจองใบใด

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `seat_key` | `string(30)` | รหัสตำแหน่งที่นั่งเฉพาะ (Unique Key) เช่น `V_1`, `A_5`, `BOXA_1` |
| `is_booked` | `boolean` | สถานะการจอง (`true` = จองแล้ว, `false` = ว่าง) / ค่าเริ่มต้น: `false` |
| `booked_at` | `timestamp` | วันเวลาที่มีการทำรายการจองล่าสุด |
| `booking_id` | `bigint unsigned` | ลิงก์ไปยังรหัสการจอง (`likay_bookings.id`) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่สร้างและอัปเดตข้อมูล |

### 2. ตาราง `likay_bookings` (จัดเก็บข้อมูลและสลิปการจองของลูกค้า)
ทำหน้าที่บันทึกประวัติข้อมูลลูกค้าที่จองพร้อมหลักฐานสลิปโอนเงินและการคำนวณราคายอดรวม

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `first_name` | `string(100)` | ชื่อของลูกค้าผู้เข้าชม |
| `last_name` | `string(100)` | นามสกุลของลูกค้าผู้เข้าชม |
| `phone` | `string(20)` | เบอร์โทรศัพท์สำหรับติดต่อกลับ |
| `booker_name` | `string(100)` | ชื่อของเจ้าหน้าที่แอดมินผู้คลิกทำรายการให้ลูกค้า |
| `slip_path` | `string` | ที่อยู่ไฟล์สลิปหลักฐานการโอนเงิน (จัดเก็บใน `storage/app/public/likay-slips/`) |
| `total_price` | `unsigned int` | ราคารวมทั้งหมดของใบจองนี้ (บาท) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่ทำรายการจอง |

### 3. ตาราง `likay_zone_prices` (จัดเก็บโครงสร้างราคาแยกตามโซน)
ทำหน้าที่เก็บราคาบัตรของแต่ละโซนสี ปรับปรุงค่าผ่านหน้าผู้บริหาร (Manager) ได้สดๆ

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `zone` | `string(20)` | ชื่อรหัสโซนเฉพาะ (Unique) เช่น `yellow`, `blue`, `pink`, `green`, `purple`, `box` |
| `price` | `unsigned int` | ราคาตั๋วในโซนนั้นๆ (บาท) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่สร้างและอัปเดตข้อมูล |

*หมายเหตุ: ราคาเริ่มต้นที่กำหนดไว้ในไฟล์ Migration (Database Seeds) มีดังนี้:*
- `purple` (แถว C, B, A ติดเวทีหน้าสุด) = **5,000 บาท**
- `green` (แถว H, G, F, E, D โซนกลางค่อนหน้า) = **2,500 บาท**
- `pink` (แถว N, M, L, K, J โซนกลางค่อนหลัง) = **2,000 บาท**
- `blue` (แถว S, R, Q, P โซนหลัง) = **1,500 บาท**
- `yellow` (แถว V, W, U, T โซนหลังสุดและด้านบน) = **1,000 บาท**
- `box` (กล่องโซนพิเศษด้านซ้ายและขวา BOX A - F) = **1,500 บาท**
- *โซนจองพิเศษอื่นๆ ในตารางราคา (vvip, vip, box_b) ถูกตั้งต้นไว้ที่ 8,000 บาท (สำหรับเก้าอี้จัดแสดง)*

---

## 🗺️ การแบ่งโซนและผังที่นั่ง (Seating Layout & Zones)

ผังที่นั่งของโรงละครถูกออกแบบให้รองรับที่นั่งทั้งหมด **586 ที่นั่ง** คำนวณและแจกแจงแบบไดนามิกผ่าน `LikaySeatMap` โดยแบ่งโซนการแสดงออกเป็นดังนี้:

1. **แถวบนสุดและด้านหลังพิเศษ (Upper Rows V, W):**
   - **แถว V (16 ที่นั่ง):** สีเหลือง (`z-yellow`) โซนบนสุดแถวหน้าสุด
   - **แถว W (18 ที่นั่ง):** สีเหลือง (`z-yellow`) โซนบนสุดแถวด้านหลังสุด
2. **แถวหลักในฮอลล์โรงละคร (Main Rows A - U):**
   - เรียงแถวตามแนวตั้งจากหน้าสุดใกล้เวที (แถว A) ไปจนถึงแถวหลังสุดใกล้ทางออก (แถว U)
   - เก้าอี้แต่ละแถวจะถูกแบ่งออกเป็น **4 กลุ่มหลัก** (ซ้ายสุด, กลางซ้าย, กลางขวา, ขวาสุด) คั่นด้วยทางเดินเดินชม
   - **โซนแถวและสีแบ่งตามระดับราคา:**
     - **แถว C, B, A (81 ที่นั่ง):** โซนสีม่วง (`z-purple`) ราคาพรีเมียมติดเวที
     - **แถว H, G, F, E, D (153 ที่นั่ง):** โซนสีเขียว (`z-green`) โซนหน้าและกลางฮอลล์
     - **แถว N, M, L, K, J (155 ที่นั่ง):** โซนสีชมพู (`z-pink`) โซนกลางฮอลล์
     - **แถว S, R, Q, P (99 ที่นั่ง):** โซนสีฟ้า (`z-blue`) โซนท้ายฮอลล์
     - **แถว U, T และ V/W (70 ที่นั่ง):** โซนสีเหลือง (`z-yellow`) โซนด้านหลังสุดและแถวบนพิเศษ
3. **โซนกล่องด้านข้างระดับ VIP (Side BOX A - F):**
   - ตั้งอยู่บริเวณระเบียงด้านซ้ายและขวาของโรงละคร (ใช้นามสกุล `BOXA` ถึง `BOXF` พร้อมรหัสที่นั่งเฉพาะ)
   - ถูกจัดประเภทเป็น โซนกล่องสีขาวพิเศษ (`z-box`) รวม **28 ที่นั่ง** ทุกเก้าอี้มีราคา **1,500 บาท**
   - **♿ สัญลักษณ์ Wheelchair:** แสดงตำแหน่งพิเศษสำหรับผู้ใช้รถเข็นบริเวณข้างกล่อง C และ F เพื่อความสวยงามและตามหลักสากล

---

## 🔗 ระบบเส้นทางควบคุม (Route Configuration)

เส้นทางประมวลผลและการเรียกข้อมูลที่นั่งทั้งหมดถูกตั้งค่าไว้ภายใต้ `LikayLiveInTheaterController` ใน `routes/web.php`:

```php
// ─── Likay Live In The Theater Seating ───────────────────────────────────────
// หน้าสาธารณะสำหรับผู้ชมทั่วไป (อัปเดตแบบเรียลไทม์ / Read-Only)
Route::get('/LikayLiveInTheater/view',  [LikayLiveInTheaterController::class, 'publicView'])->name('likay.public');

// การยืนยันสิทธิ์เข้าใช้งานระบบจอง (Authentication)
Route::get('/LikayLiveInTheater/login', [LikayLiveInTheaterController::class, 'showLogin'])->name('likay.login');
Route::post('/LikayLiveInTheater/login', [LikayLiveInTheaterController::class, 'doLogin'])->name('likay.login.post');
Route::post('/LikayLiveInTheater/logout', [LikayLiveInTheaterController::class, 'doLogout'])->name('likay.logout');

// หน้าหลักระบบผังที่นั่งแอดมิน (ต้องผ่านการสิทธิ์ล็อกอิน)
Route::get('/LikayLiveInTheater', [LikayLiveInTheaterController::class, 'index'])->name('likay.index');

// ส่งข้อมูลเพื่อสร้างใบจองพร้อมรายละเอียดลูกค้าและภาพสลิป
Route::post('/LikayLiveInTheater/book',     [LikayLiveInTheaterController::class, 'bookSeat'])->name('likay.book');

// บรอดแคสต์สถานะการคลิกเก้าอี้เรียลไทม์ (เมื่อแอดมินคนใดกำลังคลิกเลือก/ปล่อยเก้าอี้)
Route::post('/LikayLiveInTheater/select',   [LikayLiveInTheaterController::class, 'selectSeat'])->name('likay.select');
Route::post('/LikayLiveInTheater/deselect', [LikayLiveInTheaterController::class, 'deselectSeat'])->name('likay.deselect');

// ดึงรายละเอียดข้อมูลผู้จองเมื่อคลิกเก้าอี้ที่ถูกจองแล้ว (Popup Detail)
Route::get('/LikayLiveInTheater/booking-info/{seatKey}', [LikayLiveInTheaterController::class, 'bookingInfo'])->name('likay.booking-info');

// หน้าต่างแสดงตารางสรุปรายการจองทั้งหมด ค้นหาข้อมูล และพิมพ์ใบจอง
Route::get('/LikayLiveInTheater/bookings', [LikayLiveInTheaterController::class, 'bookingList'])->name('likay.bookings');

// ปรับปรุงแก้ไขโครงสร้างราคาตามแต่ละโซน (สิทธิ์ Manager เท่านั้น)
Route::post('/LikayLiveInTheater/prices', [LikayLiveInTheaterController::class, 'updatePrices'])->name('likay.prices');

// ยกเลิกรายการจองของลูกค้า ปล่อยเก้าอี้คืน และลบภาพสลิป (สิทธิ์ Manager เท่านั้น)
Route::delete('/LikayLiveInTheater/booking/{id}', [LikayLiveInTheaterController::class, 'cancelBooking'])->name('likay.cancel');

// รีเซ็ตล้างผังการจองทั้งหมดให้กลับเป็นที่นั่งว่าง 100% (สิทธิ์ Manager เท่านั้น)
Route::post('/LikayLiveInTheater/reset', [LikayLiveInTheaterController::class, 'resetSeats'])->name('likay.reset');
```

---

## ⚙️ การทำงานส่วนควบคุม (Controller Logic)

ระบบหลังบ้านใช้ `LikayLiveInTheaterController` ในการควบคุมความปลอดภัยและประมวลผลข้อมูล:

### 1. การควบคุมสิทธิ์การเข้าถึง (Access Control & Roles)
- กำหนดสิทธิ์ผู้ใช้งานผ่านสองบทบาทหลัก: `ROLE_LIKAY` (แอดมินลิเกสำหรับจอง/ดูข้อมูล) และ `ROLE_MANAGER` (ผู้บริหารระบบที่สามารถเปลี่ยนราคา ยกเลิกจอง หรือรีเซ็ตข้อมูลได้)
- การล็อกอินจะบันทึกสถานะลงใน Session Key เฉพาะคือ `likay_user_id` เพื่อป้องกันสิทธิ์สับสนกับแอดมินระบบหลัก

### 2. ระบบธุรกรรมการจองและป้องกันการแย่งที่นั่ง (Safe Transactional Booking)
- เมื่อมีคำร้องขอบันทึกการจองเข้ามาที่ฟังก์ชัน `bookSeat()` ระบบจะทำการตรวจสอบรหัสเก้าอี้และโซนสีให้สอดคล้องกับ `LikaySeatMap`
- ใช้ระบบ **Database Transaction** คู่กับคำสั่งล็อกข้อมูลข้ามรอบการทำงาน **`lockForUpdate()`** เพื่อดึงสถานะเก้าอี้ปัจจุบันออกมาตรวจสอบแบบ Exclusive
- หากมีผู้ใช้อื่นจองเก้าอี้ตัวดังกล่าวตัดหน้าไปเสี้ยววินาที ระบบจะทำรายการ **Rollback** ยกเลิกทรานแซกชันทันที ลบไฟล์ภาพสลิปที่เพิ่งอัปโหลด และส่งรหัสผิดพลาด `409 Conflict` แจ้งเก้าอี้ที่ถูกจองไปแล้วให้แอดมินทราบทันทีเพื่อความปลอดภัยขั้นสูง

### 3. การจัดการสลิปเงินโอน (Slip File Management)
- สลิปการโอนเงินจะถูกคัดกรองความปลอดภัย (Validation) ว่าเป็นไฟล์รูปภาพหรือ PDF และจำกัดขนาดไม่เกิน 5MB
- ไฟล์จะถูกเปลี่ยนชื่อและจัดเก็บอย่างเป็นระเบียบภายใน `storage/app/public/likay-slips/`
- เมื่อมีการยกเลิกการจองหรือรีเซ็ตระบบโดยผู้จัดการ (Manager) ไฟล์สลิปที่เกี่ยวข้องในระบบจัดเก็บไฟล์จะถูกตรวจสอบและลบออกจากระบบจริงทันทีเพื่อประหยัดพื้นที่ดิสก์

### 4. ระบบบรอดแคสต์สถานะเรียลไทม์ (Broadcasting Events)
- ทุกๆ การดำเนินการที่สำเร็จ ไม่ว่าจะเป็นการบันทึกการจองสำเร็จ (`bookSeat`), การยกเลิกการจอง (`cancelBooking`), การกำลังเลือกเก้าอี้ (`selectSeat`) หรือการปล่อยมือ (`deselectSeat`) จะมีการยิง Event ชื่อ `LikaySeatStatusUpdated` ไปทางช่องสัญญาณ `likay-concert`
- ทำให้แอดมินคนอื่นๆ และหน้าสาธารณะ (Public Monitor) ได้รับทราบความเปลี่ยนแปลงทันทีโดยไม่ต้องรีเฟรชหน้าเว็บ

---

## 🎨 คุณสมบัติเด่นและการทำงานบนหน้าจอ UI (Frontend Interactive Features)

หน้าเพจได้รับการออกแบบให้สวยงาม สไตล์พรีเมียมหรูหรา และใช้งานง่ายที่สุดผ่าน Vanilla JavaScript:

* **ระบบการสลับสถานะทันใจ (Optimistic UI Update with Rollback):**
  - ทันทีที่ผู้ใช้งานคลิกเลือกเก้าอี้ หน้าจอจะเปลี่ยนสถานะเก้าอี้เป็นสีเหลืองอ่อนสัญลักษณ์ "กำลังจอง" ทันทีเพื่อความลื่นไหลสูงสุด จากนั้นจึงยิง Ajax ไปลงทะเบียนที่ฝั่ง Backend
  - หากการตอบสนองจากเซิร์ฟเวอร์แจ้งข้อผิดพลาด (เช่น ที่นั่งโดนจองไปแล้ว) ระบบจะดึงสถานะจริงมา **Rollback** สลับสีเก้าอี้คืนค่าเดิมให้โดยอัตโนมัติพร้อมหน้าต่างแจ้งเตือน
* **ระบบแชร์สถานะเก้าอี้แบบเรียลไทม์ (Real-time Selecting Monitor):**
  - ระบบที่นั่งสามารถตรวจจับได้ว่าแอดมินคนอื่นบนอุปกรณ์ตัวอื่น กำลังคลิกพิจารณาเลือกเก้าอี้ใดอยู่ โดยเก้าอี้ตัวนั้นจะเปลี่ยนเป็น **สีกระพริบไฮไลท์สีส้มเรียลไทม์** ในหน้าแอดมินคนอื่นทันที
  - เมื่อแอดมินคนนั้นปล่อยเก้าอี้หรือปิดแท็บลง ระบบจะสลับสีคืนให้ทันทีผ่าน Pusher และพฤติกรรม `sendBeacon` ตอนปิดหน้าต่าง
* **ตัวคำนวณและสรุปข้อมูลสด (Interactive Booking Basket):**
  - ด้านล่างของจอภาพแสดงตะกร้าของเก้าอี้ทั้งหมดที่เรากำลังคลิกเลือก พร้อมสรุปราคาแต่ละตัว แยกตามประเภทโซน และรวมยอดเงินที่ต้องโอนให้อัตโนมัติ (Dynamic Live Cost Calculations)
* **หน้าสรุปความคืบหน้า (Live Statistics Bar):**
  - แถบความคืบหน้าแสดงสัดส่วนที่นั่งที่ถูกจองแล้วเทียบกับความจุ 586 ที่นั่ง พร้อมแสดงเปอร์เซ็นต์ความสำเร็จแบบเรียลไทม์ เหมาะสำหรับการเปิดเป็นจอมอนิเตอร์ในห้องควบคุมคอนเสิร์ต
* **หน้าจอมือถือและการตอบสนอง (Responsive Touch Optimization):**
  - ผังที่นั่งสามารถขยาย หรืิอซูมผ่านหน้าจอมือถือได้อย่างสะดวก พร้อมระบบ Touch Hover เพื่อให้อุปกรณ์ระบบสัมผัสทำงานได้ราบรื่น

---

## 🚀 วิธีการทดสอบและใช้งานระบบ

### 1. ล็อกอินเข้าใช้งานระบบจอง
- เปิดหน้าล็อกอินผ่าน URL: `/LikayLiveInTheater/login`
- ใช้บัญชีผู้ใช้ระดับแอดมินลิเก (`ROLE_LIKAY`) หรือผู้จัดการระบบ (`ROLE_MANAGER`) เพื่อเข้าใช้งานหน้าผังควบคุมหลัก `/LikayLiveInTheater`

### 2. การจองที่นั่งแบบเรียลไทม์
- เลือกคลิกเก้าอี้ที่ต้องการ (คลิกทีละหลายที่นั่งพร้อมกันได้จากโซนราคาที่ต่างกัน)
- กรอกรายละเอียดข้อมูลลูกค้า ชื่อ นามสกุล และเบอร์โทรศัพท์
- แนบหลักฐานสลิปชำระเงินโอนที่กล่องอัปโหลด
- กดปุ่ม **"ยืนยันบันทึกการจอง"** → เก้าอี้จะเปลี่ยนเป็นสีเทาเข้ม (สถานะจองแล้ว)
- ลองเปิดเบราว์เซอร์อื่น (แบบไม่ระบุตัวตน) เพื่อดูหน้ารายการสด `/LikayLiveInTheater/view` จะเห็นที่นั่งถูกเปลี่ยนสถานะเป็นสีจองทันทีโดยไม่ต้องรีเฟรชหน้าจอ

### 3. การตรวจสอบรายละเอียดการจอง
- แอดมินสามารถนำเมาส์หรือนิ้วคลิกไปที่เก้าอี้สีเทาเข้ม (เก้าอี้ที่จองแล้ว)
- ระบบจะแสดง Modal กล่องรายละเอียด ปรากฏชื่อ เบอร์โทร แอดมินผู้ดูแล เวลาจอง ยอดชำระเงิน พร้อมลิงก์เปิดดูรูปภาพสลิปที่แนบมา
- หากล็อกอินด้วยสิทธิ์ผู้จัดงาน (`ROLE_MANAGER`) จะปรากฏปุ่ม **"ยกเลิกการจองที่นั่งนี้"** สีแดงเด่นชัด ช่วยให้ปลดปล่อยที่นั่งให้กลับมาว่างได้ทันที

### 4. การจัดการโครงสร้างราคาและการเคลียร์ข้อมูล
- สิทธิ์ระดับผู้จัดงาน (`ROLE_MANAGER`) สามารถกดปุ่ม **"✏️ แก้ไขราคา"** ที่มุมบนขวา เพื่อกรอกปรับราคาค่าตั๋วของแต่ละโซนสีได้สดๆ จากนั้นแถบอธิบายราคาและยอดเงินในตะกร้าสรุปจะเปลี่ยนตามฐานราคาใหม่ทันที
- ปุ่ม **"🔄 รีเซ็ตข้อมูลทั้งหมด"** สีส้มด้านบนขวา ใช้สำหรับทำการล้างระบบเพื่อเริ่มจำหน่ายบัตรรอบถัดไป โดยระบบจะเคลียร์สถานะเก้าอี้ให้ว่างทั้งหมด และทำการลบรูปภาพสลิปในดิสก์จริงออกอย่างหมดจดเมื่อกดยืนยันคำขอ
