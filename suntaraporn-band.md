# 🎵 ระบบผังที่นั่งคอนเสิร์ต วงสุนทราภรณ์ (Suntaraporn Band Seating Chart System)

ระบบผังที่นั่งแบบโต้ตอบ (Interactive Seating Chart System) สำหรับคอนเสิร์ตวงสุนทราภรณ์ พัฒนาด้วย **Laravel Framework (Blade/Eloquent)** ร่วมกับ **Vanilla JavaScript** และ **CSS Grid/Flexbox** ช่วยให้สามารถเลือกจองที่นั่ง ปรับเปลี่ยนราคาแยกตามโซน และจัดการระบบจองได้อย่างเรียลไทม์

---

## 🏗️ โครงสร้างไฟล์ในระบบ (System Architecture)

ระบบประกอบด้วยไฟล์หลักๆ ดังต่อไปนี้:

1. **เส้นทาง (Routes):** `routes/web.php` (บรรทัดที่ 55-62)
2. **คอนโทรลเลอร์ (Controller):** `app/Http/Controllers/SuntarapornBandController.php`
3. **โมเดล (Model):** `app/Models/SuntarapornSeat.php`
4. **ไฟล์ไมเกรชัน (Database Migrations):** `database/migrations/2026_05_28_000010_create_suntaraporn_concert_tables.php`
5. **หน้าแสดงผล (Blade View):** `resources/views/suntaraporn-band.blade.php`

---

## 🗄️ โครงสร้างฐานข้อมูล (Database Schema)

ระบบใช้ตารางในฐานข้อมูล 2 ตารางเพื่อการบริหารจัดการที่นั่งและราคาแยกตามโซน:

### 1. ตาราง `suntaraporn_seats` (จัดเก็บข้อมูลการจองที่นั่ง)
ทำหน้าที่เก็บสถานะของแต่ละที่นั่งที่ถูกจอง

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `seat_key` | `string(30)` | รหัสตำแหน่งที่นั่งเฉพาะ (Unique Key) เช่น `VL_9`, `U_1`, `BOXB_9` |
| `is_booked` | `boolean` | สถานะการจอง (`true` = จองแล้ว, `false` = ว่าง) / ค่าเริ่มต้น: `false` |
| `booked_at` | `timestamp` | วันเวลาที่มีการทำรายการจองล่าสุด |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่สร้างและอัปเดตข้อมูล |

### 2. ตาราง `suntaraporn_zone_prices` (จัดเก็บราคาแยกตามโซน)
ทำหน้าที่เก็บราคาบัตรของแต่ละโซน ซึ่งอัปเดตได้ผ่านระบบ Admin

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `zone` | `string(20)` | ชื่อโซนเฉพาะ (Unique) เช่น `vvip`, `vip`, `pink`, `yellow` |
| `price` | `unsigned int` | ราคาตั๋วในโซนนั้นๆ (บาท) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่สร้างและอัปเดตข้อมูล |

*หมายเหตุ: ค่าตั้งต้นเมื่อ Run Migration มีดังนี้:*
- `vvip`, `vip`, `box_b` = **8,000 บาท**
- `purple` = **3,500 บาท**
- `green` = **3,000 บาท**
- `pink` = **2,500 บาท**
- `blue` = **2,000 บาท**
- `yellow` = **1,500 บาท**

---

## 🗺️ การแบ่งโซนและผังที่นั่ง (Seating Layout & Zones)

ผังที่นั่งถูกออกแบบให้รองรับที่นั่งทั้งหมด **542 ที่นั่ง** (ไม่รวมที่นั่งสำหรับผู้พิการและโซนพิเศษ) โดยแบ่งพื้นที่ออกเป็น:

1. **โซน VIP / VVIP (ด้านบนสุดใกล้เวที):**
   - **VVIP Box:** โซนกล่องตรงกลางด้านบนสุด
   - **VIP Left (VL):** โซน VIP ด้านซ้าย จำนวน 17 ที่นั่ง
   - **VIP Right (VR):** โซน VIP ด้านขวา จำนวน 17 ที่นั่ง
2. **โซนแถวหลัก (Main Rows A - U):**
   - เรียงลำดับจากหน้าไปหลัง (แถว A ใกล้เวทีที่สุด เรียงไปจนถึงแถว U ด้านหลังสุด)
   - แถวแต่ละแถวจะถูกแบ่งออกเป็น **4 ส่วนย่อย** (ซ้ายสุด, กลางซ้าย, กลางขวา, ขวาสุด) พร้อมมีทางเดินตรงกลางคั่น
   - **โซนราคาตามแถว:**
     - **แถว U, T, S:** โซนสีเหลือง (`z-yellow`)
     - **แถว R, Q, P:** โซนสีฟ้า (`z-blue`)
     - **แถว N, M, L, K:** โซนสีชมพู (`z-pink`)
     - **แถว J, H, G, F:** โซนสีเขียว (`z-green`)
     - **แถว E, D, C, B, A:** โซนสีม่วง (`z-purple`)
3. **โซนกล่องด้านข้าง (BOX A - F):**
   - **BOX A, D:** โซนสีเขียว (`z-green`) อยู่ด้านข้างแถว E-G
   - **BOX B:** โซนสีเทา (`z-box_b` / ราคาพิเศษแยกต่างหาก) อยู่ด้านข้างแถว J-M
   - **BOX E:** โซนสีชมพู (`z-pink`) อยู่ฝั่งตรงข้ามแถว J-M
   - **BOX C, F (โซนผู้พิการ / ♿ Wheelchair):** มีระบบการจองแบบล็อคเฉพาะ (Reserved) ใช้รหัสโซน `wc` (ราคาแสดงผลพิเศษไม่สามารถจองทั่วไปได้)

---

## 🔗 ระบบเส้นทางควบคุม (Route Configuration)

เส้นทางของระบบผังที่นั่ง ถูกกำหนดไว้ใน `routes/web.php` ภายใต้การควบคุมของ `SuntarapornBandController`:

```php
// ดึงข้อมูลผังที่นั่งและหน้าแสดงผลหลัก
Route::get('/SuntarapornBand', [SuntarapornBandController::class, 'index'])->name('suntaraporn.index');

// ส่งข้อมูลขอจอง/ยกเลิกจองที่นั่ง (Toggle)
Route::post('/SuntarapornBand/book', [SuntarapornBandController::class, 'bookSeat'])->name('suntaraporn.book');

// บันทึกแก้ไขราคาโซนบัตรใหม่ทั้งหมด
Route::post('/SuntarapornBand/prices', [SuntarapornBandController::class, 'updatePrices'])->name('suntaraporn.prices');

// รีเซ็ตการจองที่นั่งทั้งหมดเป็นค่าว่าง
Route::post('/SuntarapornBand/reset', [SuntarapornBandController::class, 'resetSeats'])->name('suntaraporn.reset');
```

---

## ⚙️ การทำงานส่วนควบคุม (Controller Logic)

ระบบควบคุมใน `SuntarapornBandController` มีฟังก์ชันหลักดังนี้:

### 1. ฟังก์ชัน `index()`
- ดึงข้อมูลที่นั่งที่ถูกจองทั้งหมดจากฐานข้อมูล (`is_booked = true`) เพื่อนำไปไฮไลท์สีเทาเข้มในระบบ
- ดึงข้อมูลราคาโซนแต่ละโซนจากตาราง `suntaraporn_zone_prices`
- ส่งข้อมูลไปยังไฟล์ View `suntaraporn-band.blade.php`

### 2. ฟังก์ชัน `bookSeat(Request $request)`
- ตรวจสอบ `seat_key` ที่ส่งมา
- ใช้ระบบ **Toggle Logic** (หากตรวจสอบว่าที่นั่งถูกจองอยู่แล้ว -> จะเปลี่ยนเป็นสถานะว่าง `is_booked = false` แต่หากเป็นที่นั่งว่าง -> จะบันทึกการจองใหม่เป็น `is_booked = true` พร้อมเก็บเวลาปัจจุบันลงใน `booked_at`)
- รองรับการสร้างแถวข้อมูลใหม่แบบ Dynamic หากไม่มีที่นั่งนั้นในตาราง (`updateOrCreate`)

### 3. ฟังก์ชัน `updatePrices(Request $request)`
- รับค่าราคารวมของทั้ง 8 โซนหลัก (`vvip`, `vip`, `box_b`, `yellow`, `blue`, `pink`, `green`, `purple`)
- ทำการ Validation ข้อมูลว่าเป็นเลขจำนวนเต็มที่มีค่ามากกว่าหรือเท่ากับ 0
- บันทึกการเปลี่ยนแปลงราคากลับเข้าไปยังฐานข้อมูล

### 4. ฟังก์ชัน `resetSeats()`
- ทำการล้างสถานะการจองของที่นั่งทุกตัวในระบบให้กลับเป็นค่าว่าง (`is_booked = false`, `booked_at = null`) เพื่อเริ่มต้นใหม่

---

## 🎨 คุณสมบัติเด่นและการทำงานบนหน้าจอ UI (Frontend Interactive Features)

หน้าแสดงผลหลักพัฒนาด้วยเทคโนโลยีที่ทันสมัย ออกแบบให้ใช้งานง่ายและสะดุดตา:

* **ระบบจองแบบ Real-time (Optimistic UI Update):**
  - เมื่อผู้ใช้งานคลิกเลือกหรือยกเลิกที่นั่ง ระบบจะสลับสถานะในหน้าจอทันทีเพื่อความรวดเร็วสูงสุด พร้อมยิงคำขอ Ajax (`fetch API`) ไปยัง Laravel Backend
  - หากระบบ Backend เกิดข้อผิดพลาดหรือที่นั่งถูกเลือกไปก่อนหน้าโดยผู้ใช้อื่น ระบบจะทำการ **Rollback** สถานะที่นั่งบนจอภาพกลับมาอัตโนมัติ และแสดงกล่องแจ้งเตือนทันที
* **แถบสรุปสถิติสด (Live Stats & Summary Bar):**
  - แสดงจำนวนที่นั่งทั้งหมด (542 ที่นั่ง), ที่นั่งที่ถูกจองแล้ว, และที่นั่งว่าง ซึ่งจะอัปเดตตัวเลขสดทันทีเมื่อผู้ใช้งานคลิกจอง
  - มีกล่อง **"สรุปการจอง"** แสดงจำนวนที่นั่งที่ผู้ใช้กำลังทำการเลือกจองอยู่ พร้อมแจกแจงรหัสที่นั่ง ราคาต่อโซน และคำนวณยอดเงินรวมให้อัตโนมัติ (Live Price Summing)
* **กล่องควบคุมตั้งค่าราคาแบบ Pop-up (Interactive Price Editor):**
  - Admin สามารถคลิกปุ่ม **"✏️ แก้ไขราคา"** เพื่อเปิด Modal ปรับเปลี่ยนราคาแต่ละโซนได้ทันที
  - เมื่อกดบันทึก ข้อมูลราคาใหม่จะถูกบันทึกลงฐานข้อมูลผ่าน Ajax และค่าตัวเลขราคาในโซนคำอธิบายด้านล่าง (Legend) จะเปลี่ยนตามทันทีโดยไม่ต้อง Refresh หน้าเพจ
* **ระบบความปลอดภัย:**
  - ทุกๆ การทำธุรกรรมผ่าน Ajax ในระบบจะมีการตรวจสอบและแนบ `X-CSRF-TOKEN` ไปใน Headers เพื่อป้องกันภัยคุกคามประเภท CSRF

---

## 🚀 วิธีการทดสอบและใช้งานระบบ

1. เข้าชมหน้าเว็บหลักผ่านที่อยู่ URL: `/SuntarapornBand`
2. **การทดสอบจองที่นั่ง:**
   - นำเมาส์ไปวางบนเก้าอี้ที่นั่ง จะเกิด Effect ขยายตัว (`scale-125`) และมีเงา (Shadow)
   - คลิกที่นั่งที่ว่าง (ตามสีโซนต่างๆ) ที่นั่งจะเปลี่ยนเป็นสีเทาเข้ม (`#555`) ทันที พร้อมรหัสที่นั่งจะไปปรากฏที่กล่องสรุปการจองด้านล่างพร้อมยอดเงินรวมที่คำนวณใหม่
3. **การทดสอบแก้ไขราคา:**
   - คลิกปุ่ม **"✏️ แก้ไขราคา"** ด้านขวาบน
   - ใส่ราคาใหม่ให้กับแต่ละโซน แล้วกด **"บันทึก"**
   - สังเกตราคาที่หน้าผังที่นั่งและยอดรวมในส่วนการจองจะปรับเปลี่ยนทันทีตามโครงสร้างราคาใหม่
4. **การทดสอบรีเซ็ตข้อมูล:**
   - คลิกปุ่ม **"🔄 รีเซ็ต"** ด้านบน
   - เมื่อกดยืนยันในกล่องตอบตกลง สถานะเก้าอี้ทุกที่นั่งจะกลายเป็นค่าว่าง (เปิดให้จองใหม่) ทั้งบนจอและในฐานข้อมูล
