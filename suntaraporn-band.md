# 🎵 ระบบผังที่นั่งคอนเสิร์ต วงสุนทราภรณ์ (Suntaraporn Band Seating Chart System)

ระบบผังที่นั่งแบบโต้ตอบ (Interactive Seating Chart System) สำหรับคอนเสิร์ตวงสุนทราภรณ์ พัฒนาด้วย **Laravel Framework (Blade/Eloquent/Broadcasting)** ร่วมกับ **Vanilla JavaScript** และ **CSS Grid/Flexbox** รองรับการทำงานแบบเรียลไทม์ (Real-time) ระบบจองที่นั่งสำหรับเจ้าหน้าที่ การชำระเงินพร้อมอัปโหลดหลักฐาน และการจัดการราคาแยกตามโซน

---

## 🏗️ โครงสร้างไฟล์ในระบบ (System Architecture)

ระบบประกอบด้วยไฟล์หลักๆ ดังต่อไปนี้:

1. **เส้นทาง (Routes):**
   - `routes/web.php` (การกำหนดสิทธิ์การเข้าถึงและ Endpoint ทั้งหมด 13 เส้นทาง)
2. **คอนโทรลเลอร์ (Controller):**
   - `app/Http/Controllers/SuntarapornBandController.php` (ควบคุม Logic ทั้งหมด ตั้งแต่การยืนยันตัวตน การจอง ไปจนถึงระบบจองแบบเรียลไทม์)
3. **โมเดล (Models):**
   - `app/Models/SuntarapornSeat.php` (จัดการเก้าอี้และสถานะจอง)
   - `app/Models/SuntarapornBooking.php` (จัดการประวัติการจองและข้อมูลผู้จองพร้อมไฟล์แนบสลิป)
4. **บริการ (Service Helper):**
   - `app/Services/SuntarapornSeatMap.php` (จัดเก็บผังเก้าอี้ 586 ที่นั่ง และข้อมูลโซนที่นั่งอย่างเป็นระบบและปลอดภัย)
5. **การถ่ายทอดสัญญาณเรียลไทม์ (Event/Broadcasting):**
   - `app/Events/SeatStatusUpdated.php` (สำหรับกระจายข้อมูลการเลือก/จองเก้าอี้ผ่าน Pusher)
   - `routes/channels.php` (กำหนดสิทธิ์ช่องทางการแชร์ความเคลื่อนไหว)
6. **หน้าแสดงผล (Blade Views):**
   - `resources/views/suntaraporn-band.blade.php` (หน้าจอหลักสำหรับเจ้าหน้าที่ - แผนผังที่นั่งและระบบจัดการจอง)
   - `resources/views/suntaraporn-public.blade.php` (หน้าจอสำหรับลูกค้าสาธารณะ - ดูสถานะที่นั่งแบบเรียลไทม์เท่านั้น ไม่สามารถกดจองได้)
   - `resources/views/suntaraporn-login.blade.php` (หน้าจอยืนยันตัวตนสำหรับเจ้าหน้าที่)
   - `resources/views/suntaraporn-bookings.blade.php` (หน้าจอรายการข้อมูลการจองทั้งหมด ค้นหาข้อมูล และการยกเลิกจองสำหรับผู้จัดการ)
7. **ไฟล์ไมเกรชัน (Database Migrations):**
   - `2026_05_28_000010_create_suntaraporn_concert_tables.php` (สร้างตารางเก้าอี้และราคาเริ่มต้น)
   - `2026_05_30_000001_create_suntaraporn_bookings_table.php` (สร้างตารางการจองและเพิ่มความเชื่อมโยงกับตารางเก้าอี้)
   - `2026_06_01_000001_update_suntaraporn_zone_prices_and_add_box.php` (ปรับปรุงโครงสร้างโซน Box)
   - `2026_06_01_000003_fix_suntaraporn_band_prices.php` (ปรับปรุงและแก้ไขราคาให้ตรงตามข้อกำหนด)
   - `2026_06_01_000004_set_vip_and_box_prices_to_zero.php` (อัปเดตราคาโซน VIP และโซนพิเศษเป็น 0 บาท เนื่องจากไม่จำหน่ายทั่วไป)

---

## 🗄️ โครงสร้างฐานข้อมูล (Database Schema)

ระบบใช้ตารางในฐานข้อมูล 3 ตารางหลักในการขับเคลื่อนระบบจอง:

### 1. ตาราง `suntaraporn_seats` (ข้อมูลที่นั่ง)
เก็บข้อมูลตำแหน่งที่นั่งและประวัติการทำรายการ

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `seat_key` | `string(30)` | รหัสตำแหน่งเก้าอี้เฉพาะตัว (Unique Key) เช่น `A_1`, `W_10`, `V_5`, `BOX_1` |
| `is_booked` | `boolean` | สถานะการจองจริง (`true` = จองเรียบร้อย, `false` = ว่าง) |
| `booked_at` | `timestamp` | วันเวลาที่มีการจองล่าสุด |
| `booking_id` | `foreignId` | คีย์เชื่อมโยงไปยัง `suntaraporn_bookings.id` (Null On Delete) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่สร้างและอัปเดตข้อมูล |

### 2. ตาราง `suntaraporn_bookings` (ข้อมูลรายการจอง)
เก็บข้อมูลลูกค้า รายละเอียดประวัติ และหลักฐานสลิปการจอง

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `first_name` | `string(100)` | ชื่อจริงลูกค้า |
| `last_name` | `string(100)` | นามสกุลลูกค้า |
| `phone` | `string(20)` | เบอร์โทรศัพท์ผู้จอง |
| `booker_name` | `string(200)` | ชื่อของเจ้าหน้าที่ผู้ออกตั๋ว/บันทึกการจอง |
| `slip_path` | `string(500)` | พาร์ทไฟล์สลิปหรือหลักฐานชำระเงินที่เก็บไว้ในระบบ Storage (ถ้ามี) |
| `total_price` | `unsigned int` | ยอดราคารวมสุทธิของรายการจองนั้นๆ (คำนวณแบบปลอดภัยจาก Server-side) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาสร้างและอัปเดตรายการ |

### 3. ตาราง `suntaraporn_zone_prices` (ราคาบัตรตามโซน)
เก็บข้อมูลราคาแยกตามโซนสีต่างๆ สามารถตั้งค่าปรับเปลี่ยนได้ตลอดเวลาผ่านสิทธิ์ผู้จัดการ

| ชื่อฟิลด์ (Column) | ประเภทข้อมูล (Type) | คำอธิบาย (Description) |
| :--- | :--- | :--- |
| `id` | `bigint unsigned` | Primary Key |
| `zone` | `string(20)` | ชื่อโซน เช่น `yellow`, `blue`, `pink`, `green`, `purple`, `vip`, `vvip`, `box` |
| `price` | `unsigned int` | ราคาบัตรประจำโซน (บาท) |
| `created_at` / `updated_at` | `timestamps` | วันเวลาที่สร้างและอัปเดตข้อมูล |

*ราคาเริ่มต้นตามการ Migration ล่าสุด (ณ ปัจจุบัน):*
- `purple` (โซนสีม่วง แถว A-E) = **3,500 บาท**
- `green` (โซนสีเขียว แถว F-J, Box A,D) = **3,000 บาท**
- `pink` (โซนสีชมพู แถว K-N, Box E) = **2,500 บาท**
- `blue` (โซนสีฟ้า แถว P-R, Box F) = **2,000 บาท**
- `yellow` (โซนสีเหลือง แถว S-U) = **1,500 บาท**
- `vvip` / `vip` / `box` / `box_b` = **0 บาท** (เนื่องจากเป็นเก้าอี้พิเศษหรือต้องติดต่อจองภายนอกระบบเท่านั้น)

---

## 🔒 ระบบยืนยันตัวตนและจำกัดสิทธิ์ (Authentication & Guard)

ระบบของเจ้าหน้าที่ไม่ได้ใช้ Laravel Authentication แบบปกติ แต่ใช้ **Custom Session-based Authentication** เพื่อความยืดหยุ่น:
- **Session Key:** `suntaraporn_user_id`
- **สิทธิ์ที่อนุญาตให้ใช้งานระบบ (Roles):**
  1. `suntaraporn` (เจ้าหน้าที่ระดับทั่วไป - ดูผังที่นั่ง เลือกเก้าอี้ และสร้างใบจองได้)
  2. `manager` (ผู้จัดการ - ทำได้ทุกอย่าง พร้อมมีสิทธิ์พิเศษในการปรับราคา ยกเลิกใบจอง และรีเซ็ตที่นั่งทั้งหมด)

---

## 🔗 เส้นทางการควบคุมในระบบ (Route Configuration)

เส้นทางทั้งหมดถูกกำหนดใน `routes/web.php` และมีการแยกแยะความปลอดภัยตามหน้าที่รับผิดชอบ:

| ประเภท (Method) | เส้นทาง (URI Pattern) | ชื่อเส้นทาง (Route Name) | หน้าที่รับผิดชอบ (Responsibility) | สิทธิ์การเข้าถึง (Access Level) |
| :--- | :--- | :--- | :--- | :--- |
| **GET** | `/SuntarapornBand/live-state` | `suntaraporn.live-state` | คืนค่า booked + selecting ปัจจุบัน (JSON) สำหรับ polling fallback | **ทุกคน (Public)** |
| **GET** | `/SuntarapornBand/view` | `suntaraporn.public` | แสดงผังที่นั่งเรียลไทม์เวอร์ชันสาธารณะ | **ทุกคน (Public)** |
| **GET** | `/SuntarapornBand/login` | `suntaraporn.login` | แสดงหน้าล็อกอินเข้าสู่ระบบ | ทุกคน (ถ้าล็อกอินแล้วจะไปหน้าผัง) |
| **POST** | `/SuntarapornBand/login` | `suntaraporn.login.post` | ประมวลผลและสร้าง Session เจ้าหน้าที่ | ทุกคน |
| **POST** | `/SuntarapornBand/logout` | `suntaraporn.logout` | ออกจากระบบ ล้างค่า Session | เฉพาะเจ้าหน้าที่ที่ล็อกอินแล้ว |
| **GET** | `/SuntarapornBand` | `suntaraporn.index` | แสดงผังที่นั่งหลักของฝั่งหลังบ้าน | เจ้าหน้าที่ / ผู้จัดการ |
| **POST** | `/SuntarapornBand/book` | `suntaraporn.book` | บันทึกการจองเก้าอี้พร้อมหลักฐาน | เจ้าหน้าที่ / ผู้จัดการ |
| **POST** | `/SuntarapornBand/select` | `suntaraporn.select` | ล็อคเก้าอี้ชั่วคราวขณะเลือก (ส้ม 🔒) | เจ้าหน้าที่ / ผู้จัดการ |
| **POST** | `/SuntarapornBand/deselect` | `suntaraporn.deselect` | ปลดล็อกเก้าอี้ชั่วคราวจากการเลือก | เจ้าหน้าที่ / ผู้จัดการ |
| **POST** | `/SuntarapornBand/prices` | `suntaraporn.prices` | อัปเดตราคาของแต่ละโซน | **เฉพาะผู้จัดการเท่านั้น** |
| **POST** | `/SuntarapornBand/reset` | `suntaraporn.reset` | รีเซ็ตล้างเก้าอี้ทั้งหมดให้ว่าง | **เฉพาะผู้จัดการเท่านั้น** |
| **GET** | `/SuntarapornBand/booking-info/{seatKey}`| `suntaraporn.booking-info` | ดึงข้อมูลใบจองของเก้าอี้ที่ถูกจองแล้ว | เจ้าหน้าที่ / ผู้จัดการ |
| **GET** | `/SuntarapornBand/bookings` | `suntaraporn.bookings` | แสดงหน้าประวัติค้นหาข้อมูลการจอง | เจ้าหน้าที่ / ผู้จัดการ |
| **DELETE**| `/SuntarapornBand/booking/{id}` | `suntaraporn.cancel` | ยกเลิกรายการจองและปลดล็อกเก้าอี้ | **เฉพาะผู้จัดการเท่านั้น** |

---

## ⚙️ การทำงานในส่วนควบคุม (Controller Functions)

ฟังก์ชันการทำงานหลักใน `SuntarapornBandController.php` ถูกพัฒนาขึ้นโดยคำนึงถึงความปลอดภัยและความสมบูรณ์ของข้อมูล:

1. **`showLogin()` / `doLogin()` / `doLogout()`**
   - ตรวจสอบความถูกต้องของ Username/Password ของผู้ใช้ที่มีสถานะ `is_active` และมีบทบาทอยู่ในกลุ่มที่ได้รับอนุญาต
   - เมื่อผ่านการตรวจสอบ ระบบจะทำการ Generate Session ID ใหม่เพื่อป้องกันสิทธิ์ (Session Fixation Countermeasure)
2. **`publicView()`**
   - ดึงรายชื่อคีย์เก้าอี้ที่ถูกจอง และราคาปัจจุบันจากฐานข้อมูล ส่งข้อมูลให้กับเทมเพลตสาธารณะที่ปิดฟังก์ชันความปลอดภัยส่วนตัวออก
3. **`index()`**
   - ทำหน้าที่โหลดผังที่นั่งสำหรับเจ้าหน้าที่ ตรวจสอบสถานะ User ปัจจุบัน และดึงข้อมูลราคาและการจองทั้งหมดมาแสดงผล
4. **`bookSeat(Request $request)`**
   - รองรับการจองครั้งละหลายที่นั่งพร้อมกันเพื่อความสะดวกของผู้จองและเจ้าหน้าที่
   - ทำงานภายใต้ **Database Transaction** และทำการล็อคแถวข้อมูลด้วยคำสั่ง `lockForUpdate()` เพื่อป้องกันปัญหาสิทธิ์การจองเก้าอี้ทับซ้อนกันในเสี้ยววินาที (Race Condition)
   - คำนวณยอดเงินรวมจากโครงสร้างราคาบน Server-side เสมอ เพื่อป้องกันการแก้ไขราคาตั๋วผ่านทางฝั่ง Client
   - จัดการอัปโหลดไฟล์สลิปหลักฐาน เก็บในไดเรกทอรี `suntaraporn_slips` ภายใต้ระบบดิสก์แบบ Local
   - เมื่อจองสำเร็จจะทำการส่งกระจายข้อมูลสถานะที่นั่งใหม่ (Broadcasting) ผ่าน Pusher ไปยังผู้ใช้ทุกคนทันที
5. **`selectSeat()` / `deselectSeat()`**
   - จัดการส่งสัญญาณเรียลไทม์เพื่อแจ้งผู้ใช้อื่นว่าเก้าอี้นี้กำลังมีเจ้าหน้าที่อีกคนกำลังกดเลือกอยู่ (ช่วยลดโอกาสจองชนกันก่อนกรอกฟอร์ม)
   - **`selectSeat()`** บันทึก `selectingKeys` ลงใน Laravel Cache (`suntaraporn_selecting_keys_[showDate]`, TTL 180 วิ) เพิ่มเติมจาก Pusher Broadcast
   - **`deselectSeat()`** / **`bookSeat()`** / **`resetSeats()`** ลบ keys ที่เกี่ยวข้องออกจาก Cache ทันทีเพื่อให้ข้อมูล polling สอดคล้องกับสถานะจริง
6. **`liveState()`**
   - endpoint JSON สาธารณะ (`GET /SuntarapornBand/live-state`) คืนค่า `booked` (จาก DB), `selecting` (จาก Cache) และ `sponsor` (จาก DB) ในรูปแบบ key array เพื่อนำมาอัปเดตสีเก้าอี้
   - ใช้โดย polling fallback script ในหน้าแอดมินและผู้ชมทั่วไป
7. **`updatePrices(Request $request)`**
   - เปิดให้เฉพาะผู้จัดการเป็นผู้อัปเดตราคาโซน ทั้งหมดถูก Validate ให้เป็นตัวเลขที่มีค่าตั้งแต่ 0 ขึ้นไป
7. **`resetSeats()`**
   - เปิดให้เฉพาะผู้จัดการล้างข้อมูลเก้าอี้ทั้งหมด พร้อมทั้งทำการตรวจสอบและลบไฟล์สลิปการจองที่ผูกอยู่ทั้งหมดออกจากระบบ Storage เพื่อลดขยะข้อมูล
8. **`bookingInfo($seatKey)`**
   - ดึงรายละเอียดลูกค้าและผู้รับจองเพื่อนำมาแสดงผลบน Modal เมื่อเจ้าหน้าที่กดคลิกดูที่นั่งที่เป็นสีเทาเข้ม
9. **`bookingList(Request $request)`**
   - แสดงตารางข้อมูลใบจองทั้งหมด รองรับการกรองข้อมูลแบบ Real-time ตามคำค้นหา (ชื่อ นามสกุล เบอร์โทรศัพท์ รหัสตำแหน่งเก้าอี้)
10. **`cancelBooking($id)`**
    - เปิดให้เฉพาะผู้จัดการทำการลบใบจอง ปลดล็อกเก้าอี้ทั้งหมดที่เชื่อมโยงกับใบจองนั้นให้กลับมาว่าง และทำการลบไฟล์สลิปใน Storage ทันทีเพื่อรักษาความสะอาดของระบบ

---

## 📡 ระบบเรียลไทม์และการส่งกระจายข้อมูล (Real-time & Broadcasting)

ระบบผังที่นั่งนี้มีจุดเด่นคือการอัปเดตแบบทันท่วงที โดยไม่ต้องทำการรีเฟรชหน้าจอ (Zero Refresh):
- **เครื่องมือหลัก:** ใช้บริการ **Pusher** ร่วมกับ Laravel Event Broadcasting
- **ช่องทางส่งสัญญาณ (Channel):** `suntaraporn-concert` (ช่องสัญญาณแบบสาธารณะเพื่อลด overhead ในการโหลด)
- **ประเภทของสัญญาณที่ส่ง (Broadcasting Event):** `SeatStatusUpdated` (บรรจุโครงสร้างข้อมูลคีย์เก้าอี้และสถานะ เช่น จองแล้ว ว่าง หรือกำลังเลือกอยู่)
- **Polling Fallback (สำรองกรณี Pusher ไม่ deliver):**
  - ทั้งหน้าสำหรับเจ้าหน้าที่ (`suntaraporn-band.blade.php`) และหน้าสำหรับผู้ชมทั่วไป (`suntaraporn-public.blade.php`) มีระบบ **Polling Fallback** คอยสำรองดึงข้อมูลจาก `/SuntarapornBand/live-state` ทุกๆ 5 วินาที เพื่อซิงก์สถานะที่นั่ง (Booked, Sponsor, Selecting) ในกรณีที่ Pusher ไม่เชื่อมต่อ
  - เพิ่มระบบเตือนความปลอดภัย **Expiry Warning**: หากเจ้าหน้าที่คลิกเลือกที่นั่งค้างไว้นานเกิน 3 นาที ระบบจะแสดงแถบแจ้งเตือนสีส้มที่ด้านบนแนะนำให้รีเฟรชหน้าเว็บ เพื่อป้องกันการถือสิทธิ์ที่นั่งค้างหลังจาก Cache หมดอายุบน Server
  - logic ของ polling จะ diff สถานะเก่ากับใหม่ก่อน apply เพื่อไม่ให้ re-render ที่นั่งที่ไม่มีการเปลี่ยนแปลง
  - ทำงานควบคู่กับ Pusher — ถ้า Pusher ส่งมาก่อนก็ไม่มีผล (idempotent)
- **สถานะที่นั่งบน UI:**
  - `ว่าง`: แสดงสีตามโซนปกติ (ชมพู, ฟ้า, เหลือง, เขียว, ม่วง)
  - `🔒 กำลังเลือกชั่วคราว`: แสดงสีส้มกะพริบสดใส (เมื่อมีเจ้าหน้าที่ท่านอื่นกำลังคลิกเลือกไว้ และอยู่ในขั้นตอนกรอกข้อมูลใบจอง)
  - `จองแล้ว`: แสดงสีเทาเข้ม
- **การจัดการกรณีปิดแท็บกระทันหัน (Tab/Browser Exit Prevention):**
  - ฝั่ง Frontend มีการดักจับ Event `beforeunload` และ `visibilitychange`
  - หากเจ้าหน้าที่เลือกเก้าอี้เป็นสีส้มไว้แต่ทำการปิดแถบเบราว์เซอร์หรือเครื่องขัดข้อง ระบบจะใช้ JavaScript `navigator.sendBeacon()` ในการส่งข้อมูลขอยกเลิกการเลือกชั่วคราวกลับมาที่ Server ทันที ทำให้เก้าอี้สีส้มไม่ค้างอยู่ในระบบ

---

## 🎨 คุณสมบัติและส่วนติดต่อผู้ใช้งานหลัก (UI Interactive Features)

- **Optimistic UI Update:** เมื่อผู้ใช้คลิกเลือกหรือปล่อยเก้าอี้ ระบบจะเปลี่ยนสีเก้าอี้บนเบราว์เซอร์ทันทีและยิง Ajax ไปพร้อมกัน หากเกิดข้อผิดพลาดจากฝั่ง Server ระบบจะทำการดึงสถานะกลับมาเป็นค่าเดิม (Rollback) ทันที
- **Live Summary Bar:** ด้านล่างของจอภาพจะมีแถบคำนวณยอดเงิน รายการที่เลือกแบบละเอียด อัตราตั๋วแยกโซนสดๆ ปรับเปลี่ยนตามราคาที่ดึงจากฐานข้อมูลจริง
- **Responsive Seat Map:** ผังเก้าอี้รองรับทั้งการย่อ/ขยาย (Zoom & Pan) และการปรับแต่งสัญลักษณ์ตำแหน่งคนพิการ (Wheelchair Symbol) บริเวณข้างกล่อง C และ F ตามลักษณะแผนผังจริงที่สวยหรู

---

## 🧪 การทดสอบระบบและการตรวจสอบคุณภาพ (Testing)

ระบบ Suntaraporn Band มีการเขียน Unit และ Feature Test เพื่อความปลอดภัยและความเสถียรของฟังก์ชันการจอง โดยมีเคสทดสอบที่ผ่านกระบวนการตรวจสอบทั้งหมด **19 เคส (187 Assertions)**:

### วิธีการรันการทดสอบ
เปิดโปรแกรม Terminal ในระบบแล้วรันคำสั่งดังต่อไปนี้:
```bash
php artisan test --filter=SuntarapornBand
```

*เคสทดสอบหลักที่ครอบคลุม:*
1. **การเข้าใช้งานตามสิทธิ์:** ทดสอบว่าเฉพาะผู้ใช้ที่เป็นเจ้าหน้าที่/ผู้จัดการที่มีสิทธิ์เท่านั้นที่สามารถเปิดหน้าจัดการระบบได้
2. **การทำรายการจองเก้าอี้:** ทดสอบการจองเก้าอี้พร้อมอัปโหลดไฟล์ใบเสร็จ และการป้องกันการจองเก้าอี้ที่ถูกจองไปแล้ว
3. **การป้องกันราคาฝั่งไคลเอนต์:** ทดสอบการตรวจสอบราคาสินค้าระดับเซิร์ฟเวอร์เพื่อให้ยอดเงินถูกต้องตามฐานข้อมูลจริงเสมอ
4. **การรีเซ็ตที่นั่ง:** ทดสอบความถูกต้องของสิทธิ์ผู้จัดการในการล้างสถานะเก้าอี้และลบไฟล์สลิปขยะออกจากระบบ
5. **การยกเลิกจอง:** ทดสอบระบบการถอนการจอง คืนสถานะที่นั่ง และทำความสะอาดพื้นที่จัดเก็บไฟล์สลิปอย่างปลอดภัย
6. **การจองแยกตามรอบการแสดง:** ทดสอบว่าเก้าอี้เดียวกันสามารถจองแยกกันได้อย่างอิสระตามรอบวันที่แสดง
7. **ระบบสปอนเซอร์ (Sponsor Seats):** ตรวจสอบการจองสปอนเซอร์ การจำกัดการจองทับ และการแสดงผลแยกในหน้าแอดมิน

